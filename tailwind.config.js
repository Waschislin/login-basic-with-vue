/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx,vue}"],
  darkMode: "class", //media ou class (media busca a config automática de darkmode do navegador e o class é usado para ativar e desativar o dark mode por botão)
  theme: {
    extend: {},
  },
  plugins: [],
};
